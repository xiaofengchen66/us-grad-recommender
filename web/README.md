# web

Institution search/detail frontend for US Graduate Recommender —
Next.js + TypeScript + Tailwind. Read-only, over the API in
`../src/us_grad_recommender/api/`.

Real setup and run instructions live in the root
[`../README.md`](../README.md#running-the-frontend), not here — this file
exists only so `npm create next-app` conventions are met; don't duplicate
the setup steps in two places.
